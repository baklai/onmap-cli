const onmap = require('./utils/onmap-scan');
const cidrToArray = require('./utils/cidr-to-array');
const csvToArray = require('./utils/csv-to-array');

module.exports = { onmap, cidrToArray, csvToArray };
